# REPI — Python dependencies
# Locked versions for reproducible builds within the ZCHPC CCE, per Track 3 ToR requirement
# ("dependency locked manifests"). Update deliberately, not with floating ranges.

# --- Core / API ---
fastapi==0.111.0
uvicorn==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1

# --- Data ingestion & storage ---
requests==2.31.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
pandas==2.2.2

# --- Index computation (ML core) ---
scikit-learn==1.4.2
prophet==1.1.5
statsmodels==0.14.2

# --- Narrative synthesis layer (quantized LLM) ---
transformers==4.41.1
accelerate==0.30.1
bitsandbytes==0.43.1
torch==2.3.0

# --- Testing ---
pytest==8.2.0
pytest-cov==5.0.0
